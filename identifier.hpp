#pragma once
#ifndef __IDENTIFIER_HEADER_GUARD__
#define __IDENTIFIER_HEADER_GUARD__
#include <string>
#include <vector>

namespace drawdown{
    enum class identifier{
        
    };

    struct node{

    };

}

#endif